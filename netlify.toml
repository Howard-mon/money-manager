[build]
  command = "npm run build"
  publish = "dist"

# Must stay above the SPA catch-all, otherwise /* swallows the webhook.
[[redirects]]
  from = "/api/line/webhook"
  to = "/.netlify/functions/line-webhook"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
